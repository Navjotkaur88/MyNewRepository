package Arrays;

public class SbDemo {
    public static void main(String[] args)
    {
        //System.out.println(S1);
     //   String s2 = s1.concat("");
         // Mutable  String builder -- thread safe(Synchronized)
        // Mutable string buffer -- non thread safe(non- Synchronized)
        //thread -- small process safe do not allow
    //    ab.append("Incorp")

    }
}
