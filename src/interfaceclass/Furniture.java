package interfaceclass;

public interface Furniture {

    //Public static final - is redundant it is already there by default
    int x = 10;

    // by default public abstract
    //in abstract class we cannot create object
    //cannot create constructor interface


    //upcasting?
void show();
// Diamond problem- ?
}
